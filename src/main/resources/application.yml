spring:
  servlet:
    multipart:
      max-file-size: 10MB
  datasource:
    url: jdbc:h2:mem:test
    username: sa
    password:
    driver-class-name: org.h2.Driver
  h2:
    console:
      enabled: true
  jpa:
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
#        show_sql: true
        format_sql: true

server:
  servlet:
    session:
      tracking-modes: cookie
    encoding:
      charset: UTF-8
      force: true
      enabled: true

logging.level:
  org.hibernate.SQL: debug

file:
  dir: "/Users/jangsea-ung/Desktop/jang/private/private_project/study/board/src/main/resources/static/file/image/"
